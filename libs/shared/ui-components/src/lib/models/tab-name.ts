/**
 * NOTE: - Move to ds/models to util-common if used outside this layer
 *
 * Enum automatically creates a type
 */
export enum TabName {
  Clients = 'clients',
  Tasks = 'tasks',
  About = 'about',
}
